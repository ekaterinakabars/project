
// import fetch from 'node-fetch';
// const { request } = require('./Step3')
// jest.mock('node-fetch');
// const { Response } = jest.requireActual('node-fetch');

// describe('Positiv cases', () => {
//     test('createUser calls fetch with the right args and return pikcture', async () => {
//         fetch.mockReturnValue(Promise.resolve(new Response('4')));
//         const data = await request();

//         expect(fetch).toHaveBeenCalledTimes(1);
//         expect(fetch).toHaveBeenCalledWith('http://website.com/users', {
//             method: 'POST',
//         });
//         expect(data).toBe('4');
//     });

// })

// describe('Negativ cases', () => {
//     test('Should print an async function  ', () => {
//         expect(request()).not.toBe(" ")
//         expect(request()).not.toBe("numbers")


//     })
// })









